# v4.2 Backlog Tracker

Dokumen ini merangkum inisiasi fitur v4.2 yang diminta: plugin registry, command VS Code tambahan, starter scaffolder, dashboard performa, utilitas testing, dan helper Storybook.

## Status ringkas

| Item | Status | Implementasi awal |
|---|---|---|
| Plugin Registry | ✅ Scaffolded | `packages/plugin-registry` + CLI `tw-plugin` |
| VS Code commands | ✅ Added | `analyzeWorkspace`, `installPlugin`, `createComponent` |
| Create template CLI | ✅ Scaffolded | `packages/cli` (generator `create-tailwind-styled`) |
| Performance dashboard | ✅ Scaffolded | `packages/dashboard/src/server.mjs` |
| Testing integration | ✅ Scaffolded | `packages/testing` |
| Storybook addon | ✅ Scaffolded | `packages/storybook-addon` |

## Catatan

- Seluruh item di atas disiapkan sebagai fondasi yang buildable dalam monorepo saat ini.
- Integrasi lanjutan (marketplace publishing, UI dashboard production, template penuh per framework) tetap bisa diiterasi di PR berikutnya.
