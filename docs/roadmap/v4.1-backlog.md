# v4.1.0 Backlog Tracker

Dokumen ini mengkonsolidasikan backlog v4.1.0 agar semua item punya owner, output, dan command validasi yang jelas.

## Snapshot Prioritas

| Prioritas | Item | Status | Output Target |
|---|---|---|---|
| 🔴 Tinggi | Rust parser aktif default + fallback JS | In progress | Scanner mencoba native binding dulu lalu fallback JS aman |
| 🔴 Tinggi | Benchmark publik di README | In progress | README berisi tabel benchmark + cara reproduksi |
| 🟡 Sedang | Video demo / GIF | In progress | Runbook + transcript CLI tersedia, rekaman final pending |
| 🟡 Sedang | Hardening Next.js frontend existing | ✅ Done | Folder `examples/standar-config-next-js-app` dengan token switch, cart state, container query demo |
| 🟢 Rendah | Blog post announcement | In progress | Draft announcement siap publish |
| 🟢 Rendah | Contributing guide lengkap | In progress | CONTRIBUTING diperluas dengan alur kerja kontribusi |

## 1) Rust parser aktif default (dengan fallback)

### Acceptance criteria
- [x] Scanner mencoba native binding path terlebih dahulu saat tersedia.
- [x] Jika binding native tidak tersedia/error, scanner tetap jalan dengan parser JS.
- [x] Ada observability minimum via `TWS_DEBUG_SCANNER=1` untuk melihat mode parser.
- [x] Ditutup dengan regression test untuk memastikan fallback tidak break.

### Catatan implementasi
- Native parser saat ini sudah tersedia sebagai scaffold di `native/` dan benchmark terpisah.
- Integrasi default disarankan bertahap (opt-in dulu via env, lalu default-on setelah stabil lintas OS).

## 2) Benchmark publik di README

### Acceptance criteria
- [x] README punya section benchmark publik.
- [x] Ada command reproduksi.
- [x] Data benchmark lintas OS/Node dari CI scale benchmark dipublikasikan berkala (lokal snapshot sudah tersedia).

### Command referensi
```bash
npm run bench:massive -- --root=test/fixtures/large-project --out=artifacts/scale/massive-readme.json
```

## 3) Video demo / GIF

### Acceptance criteria
- [x] Demo CLI minimal tersedia via runbook + transcript generator.
- [ ] Demo live token switching di contoh UI.
- [ ] Asset GIF/mp4 final dimasukkan ke docs/README (runbook & transcript sudah tersedia).

### Rekomendasi tooling
- Terminalizer / asciinema untuk terminal flow.
- Screen recorder biasa untuk browser flow.

## 4) Hardening Next.js frontend existing

### Acceptance criteria
- [x] Menggunakan folder frontend existing (`examples/standar-config-next-js-app`) sebagai baseline.
- [x] Integrasi demo UI pada halaman example tanpa scaffold baru.
- [x] Menambahkan demo interaktif ringan (token/theme switch + cart state) di app existing.
- [x] Diuji end-to-end pada Next.js dev server (dengan fallback font pada lingkungan terbatas).

## 5) Blog post announcement

### Acceptance criteria
- [x] Draft blog post tersedia.
- [ ] Ditinjau untuk data benchmark final.
- [ ] Publish ke kanal resmi.

## 6) Contributing guide lengkap

### Acceptance criteria
- [x] Setup, struktur project, workflow kontribusi lebih detail.
- [x] Daftar command validasi sebelum PR.
- [x] Tambah section release process saat alur rilis final disepakati.

## Timeline Operasional (2 minggu)

| Minggu | Hari | Fokus |
|---|---|---|
| 1 | 1-2 | Rust parser integration (opt-in + fallback hardening) |
| 1 | 3 | Benchmark README + baseline data |
| 1 | 4-5 | Demo video/GIF |
| 2 | 6-7 | Hardening example Next.js existing |
| 2 | 8 | Blog post finalisasi |
| 2 | 9 | Contributing + release docs sinkronisasi |

## Definition of Done v4.1.0

Release dianggap siap jika:
1. Item prioritas tinggi selesai penuh.
2. Example Next.js existing dapat dipakai sebagai referensi frontend utama.
3. Dokumentasi user-facing (README/announcement/contributing) sinkron.
